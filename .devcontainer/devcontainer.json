{
  "name": "Nemlig Shopping Assistant",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-22-bookworm",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y xvfb fluxbox x11vnc novnc websockify && npm install && npx playwright install --with-deps chromium",
  "postStartCommand": "npm run remote:desktop",
  "forwardPorts": [6080],
  "portsAttributes": {
    "6080": {
      "label": "Remote Chromium desktop",
      "onAutoForward": "notify"
    }
  }
}
